{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["ENABLE_ROBOTS", "HOST_NAME"],
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
